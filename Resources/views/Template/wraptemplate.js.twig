/**
 * This module has been dynamically generated by using the template
 * at "{{ path|raw }}"
 */
YUI.add("{{ module }}", function (Y) {
    var tpl = new Y.Template(Y.Handlebars);

    Y.Template.register(
        "{{ module }}",
        tpl.compile("{{ templateCode }}")
    );
});
